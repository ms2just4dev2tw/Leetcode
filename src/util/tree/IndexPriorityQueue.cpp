
#include <iostream>

/**
 * 索引优先队列
 * 
 */
class IndexPriorityQueue
{
private:
    // data

    // index of data, index -> data

    // inverse of index, data -> index, pq - qp[pq[i]] = pq[qp[i]] = i

public:
    IndexPriorityQueue(/* args */);
    ~IndexPriorityQueue();
};

